# Root Environment Variables
# These act as defaults for docker-compose

# Database (used by postgres service and backend)
DB_NAME=leyladigital
DB_USER=postgres
DB_PASSWORD=postgres
DB_PORT=5432

# Backend Port
PORT=4000
